class Solution:
    def generateParenthesis(self, n):
        """
        :type n: int
        :rtype: List[str]
        """



        return output

    def back_track(self, ...):

        if self.terminating_condition_is_reached(...):
            output.append("".join(temp_arr[:]))
            return False

        for bracket in parenthesis:
            temp_arr[index] = bracket

            if not self.valid(...):
                temp_arr[index] = '.'
                continue

            back_track(...)
            temp_arr[index] = '.'

        return False

    def terminating_condition_is_reached(self, ...):

    def valid(self, ...):

        return False
